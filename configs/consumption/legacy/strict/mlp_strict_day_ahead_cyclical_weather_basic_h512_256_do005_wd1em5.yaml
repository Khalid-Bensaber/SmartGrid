experiment_name: consumption_mlp_strict_day_ahead_cyclical_weather_basic_h512_256_do005_wd1em5
problem: consumption

data:
  dataset_key: full_2020_2026
  date_col: Date
  target_name: tot

split:
  train_ratio: 0.70
  val_ratio: 0.15
  train_end_date: '2025-06-30'
  val_end_date: '2025-09-30'

features:
  forecast_mode: strict_day_ahead
  include_calendar: true
  include_temperature: true
  include_manual_daily_lags: true
  include_cyclical_time: true
  include_lag_aggregates: false
  include_recent_dynamics: false
  include_shifted_recent_dynamics: false
  include_weather: true
  weather_mode: basic
  lag_days: [7, 1, 2, 3, 4, 5, 6]

training:
  seed: 42
  batch_size: 720
  epochs: 100
  patience: 8
  learning_rate: 0.001
  weight_decay: 0.00001
  dropout: 0.05
  hidden_layers: [512, 256]
  device: auto
  num_workers: 0

artifacts:
  root_dir: artifacts
  exports_subdir: exports/consumption
  registry_subdir: models/consumption
  notebook_output_filename: notebook_export_consumption.csv
  summary_filename: run_summary.json
